<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <CheckBox
        android:id="@+id/cb_choice_correct"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:contentDescription="Correct answer" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <EditText
            android:id="@+id/et_choice_text"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/bg_tactile_input_borderless"
            android:hint="Answer text"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:singleLine="true"
            android:textColor="@color/l2e_ink"
            android:textColorHint="@color/l2e_muted" />

        <ImageView
            android:id="@+id/iv_choice_image"
            android:layout_width="72dp"
            android:layout_height="56dp"
            android:layout_marginTop="6dp"
            android:contentDescription="Answer image"
            android:scaleType="centerCrop"
            android:visibility="gone" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/btn_choice_image"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/bg_btn_secondary"
        android:contentDescription="Add answer image"
        android:padding="12dp"
        android:scaleType="centerInside"
        android:src="@android:drawable/ic_menu_gallery"
        app:tint="@color/l2e_forest" />

    <ImageButton
        android:id="@+id/btn_remove_choice"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/bg_btn_danger"
        android:contentDescription="Remove answer choice"
        android:src="@drawable/ic_delete_small"
        app:tint="@color/white" />
</LinearLayout>
